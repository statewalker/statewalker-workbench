/**
 * Shell-core activation function for dynamic loading via fragment.
 * Follows the module contract: default export (ctx) => cleanup.
 */
export { initShellCore as default } from "./init-shell-core.js";
