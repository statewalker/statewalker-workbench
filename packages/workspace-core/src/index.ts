// Placeholder. The real exports — `buildWorkspace`, `initWorkspace`,
// `startWorkspace`, the request-file-system dialog factory, and the
// `workspace:open` / `workspace:change` handlers — land in the next
// migration step when the source moves from the legacy
// `@repo/workspace-core` package in `statewalker-apps` into this package.
export {};
