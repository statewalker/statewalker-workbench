import type { FilesApi } from "@statewalker/webrun-files";
import type { Project } from "@statewalker/workspace.core";

/**
 * The project-rooted `FilesApi` a project's repository lives on.
 *
 * Step-zero stub — see the plan's ground rule 1.
 */
export function repoFilesOf(_project: Project): FilesApi {
  throw new Error("not implemented");
}
