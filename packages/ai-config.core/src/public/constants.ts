/**
 * Settings-tab identity for the "Remote Models" connections panel. The `id`
 * doubles as the deep-link anchor (`OpenSettingsCommand({ tabId })`); the
 * `viewKey` resolves the React `ViewComponent` from the `ViewRegistry` at
 * render time. They share the literal `ai-config:connections`.
 */
export const AI_CONFIG_CONNECTIONS_TAB_ID = "ai-config:connections";
export const AI_CONFIG_CONNECTIONS_TAB_VIEW_KEY = "ai-config:connections";
