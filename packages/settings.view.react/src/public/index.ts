// settings-views exposes no public React components today — the
// SettingsButton (header) and SettingsDialog (overlay) are
// contributed to `dock:header-items` / `dock:overlays` from `init`
// and resolved via `ViewRegistry`.
export {};
