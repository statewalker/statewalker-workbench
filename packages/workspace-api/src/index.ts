// Placeholder. The real exports land in step 2 of the workspace-bootstrap
// migration when the source files move from @repo/workspace-api into this
// package. Kept as a stub so package consumers and tooling can resolve the
// `.` export entry during the scaffold step.
export {};
