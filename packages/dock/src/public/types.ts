/**
 * Direction hint for `dock:show-panel`. The dock fragment translates
 * this into a DockView `addPanel` position option.
 */
export type PanelPosition = "left" | "right" | "top" | "bottom" | "within";
