/** Catalog id for `json:catalogs`. */
export const AI_LOCAL_MODELS_CATALOG_ID = "ai-local-models";

/** ViewKey + settings-tab id for the "Local Models" tab body. */
export const LOCAL_MODELS_TAB_VIEW_KEY = "ai-local-models:tab";
export const LOCAL_MODELS_TAB_ID = "ai-local-models:tab";

/** Default base path for transformers.js weight storage. Sits under
 * the workspace's system folder (`<root>/.settings`). */
export const TJS_WEIGHTS_BASE_PATH = "/.settings/models/tjs";
