/**
 * `ViewRegistry` key for the providers settings tab. Contributed
 * to `settings:tabs` by the providers fragment (logic) and bound
 * to a React component by the providers-views fragment.
 */
export const PROVIDERS_SETTINGS_TAB_VIEW_KEY = "providers:settings-tab";

/**
 * `ViewRegistry` key for the inline model picker rendered in the
 * chat composer. Wave 7.1 wires this through `chat:composer-actions`.
 */
export const PROVIDERS_MODEL_PICKER_VIEW_KEY = "providers:model-picker";
